version: '3'

volumes:
    data: {}
services:
    mariadb:
        container_name: mariadb
        image: ./requirements/mariadb
        restart: always
        expose:
            - "3306"
        networks:
            - intra
        tty: true
    nginx:
        container_name: nginx
        image: debian:buster
        restart: always
        ports:
            - "443:443"
        volumes:
            - data:/var/www/html
        networks:
            - intra
        tty: true
    wordpress:
        container_name: wordpress
        image: debian:buster
        restart: always
        volumes:
            - data:/var/www/html
        expose: 
            - "9000"
        networks:
            - intra
        tty: true
networks:
    intra:
        driver: bridge